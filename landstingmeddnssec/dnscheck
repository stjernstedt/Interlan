echo -n "<br><i>A <a href=http://dnscheck.iis.se>DNSCheck</a> against `cat landsting|wc -l` county council domains \
`date` gave a result of "
echo `cat test/* | grep "is recursive"|wc -l` recursive DNS\'s and DNSCheck also find \
`cat test/* | grep ERROR|wc -l` ERROR and \
`cat test/* | grep WARN|wc -l` WARNINGS
