

echo -n "<br><i>A <a href=http://dnscheck.iis.se>DNSCheck</a> against `cat domain|wc -l` domains gave a result of "
echo `cat test/* | grep "is recursive"|wc -l` recursive DNS and DNSCheck also found \
`cat test/* | grep ERROR|wc -l` ERROR and \
`cat test/* | grep WARN|wc -l` WARNINGS
